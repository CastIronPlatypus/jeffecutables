# jeffecutables

Personal CLI tools, organized by subdirectory.

## Setup (required on every machine)

```sh
./install.sh
```

This appends the PATH setup to your `~/.zshrc` (or `~/.bashrc`). After that, any executable placed in a subdirectory is immediately available (after `chmod +x`) in new shell sessions.
